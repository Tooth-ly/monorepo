import styled from 'styled-components';

export const Container = styled.div({
  border: 'solid 1px',
  borderRadius: '10px',
});

export const Name = styled.p({
  fontSize: '20px',
  margin: '0px',
  padding: '10px',
  cursor: 'pointer',
  textAlign: 'center',
});
