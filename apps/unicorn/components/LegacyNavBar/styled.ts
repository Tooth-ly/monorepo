import styled from 'styled-components';

export const NavContainer = styled.div({});

export const NavContainer900 = styled.div({
  position: 'fixed',
  bottom: 0,
  display: 'flex',
  width: '100%',
  justifyContent: 'space-evenly',
  backgroundColor: 'white',
});
